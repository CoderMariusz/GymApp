One-line: small uppercase status pill; always carries a word, never colour alone.

```jsx
<Badge tone="accent" icon="trophy">PR</Badge>
<Badge tone="neutral" size="sm">Warm-up</Badge>
```

For sync status use `SyncBadge` instead — it owns the six state definitions.
