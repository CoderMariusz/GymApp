One-line: dropdown for ranges and long lists — date range on Progress, units and language in Settings.

```jsx
<Select value={range} options={['This week','This month','Last 3 months']} onChange={setRange} />
```

Full-width by default, chevron on the right, 44px tall. Use `SegmentedControl` for two to four options instead.
