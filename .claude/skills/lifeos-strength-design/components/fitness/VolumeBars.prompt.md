One-line: daily/weekly training-volume bars for the Progress screen.

```jsx
<VolumeBars data={[8,12,6,14,9,17,11]} labels={['May 1','May 14','May 28']} />
```

Every chart needs a tabular alternative next to it (DESIGN-BRIEF C-9) — render a `<table>` in a details disclosure or a summary list beneath.
