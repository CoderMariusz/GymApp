One-line: the always-visible rest countdown that sits above the tab bar and never covers set entry (DESIGN-BRIEF C-7).

```jsx
<RestTimer remaining="1:45" running nextLabel="Next exercise" nextValue="Incline DB Press" />
```

It docks, it does not overlay. Pausing is one 56px target away; the timer keeps running while the user logs the next set.
